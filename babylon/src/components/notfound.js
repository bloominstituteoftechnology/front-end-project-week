import React from 'react';

const NotFound = () => (
  <div style={{ padding: '10px' }}>
    <h3 style={{ fontSize: '4rem' }}>404 page not found</h3>
    <p style={{ fontSize: '2rem' }}>We are sorry but the page you are looking for does not exist.</p>
  </div>
);

export default NotFound;
