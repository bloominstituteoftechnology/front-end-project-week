const initialState = {
	todos: [],
	addingTodo: false,
	editingTodo: false,
	deletingTodo: false,
	viewingTodo: false
};

const todosReducer = (state=initialState, action) => {
	switch(action.type) {
		// add new todo
		case:
			return ;
		// edit new todo
		case:
			return ;
		// delete new todo
		case:
			return ;
		default: 
			return state;
	}
}