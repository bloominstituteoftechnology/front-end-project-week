import React, {Component} from 'react'


class NotesList extends Component {

    constructor() {
        super();
    }

    render() {

        return (
            <div>
            {/* // {this.props.friends.map( friend => <Friend name={friend.name} key={friend.id} email={friend.email} age={friend.age} />)} */}
            Yep it works
            </div>
        )
    }
}

export default NotesList