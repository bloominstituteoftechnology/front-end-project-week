import styled from 'styled-components'
import { 
  width, 
  borders, 
  color,
  height,
  borderColor
} from 'styled-system'

export default styled.div`
  ${color}
  ${width}
  ${height}
  ${borders}
  ${borderColor}
`
