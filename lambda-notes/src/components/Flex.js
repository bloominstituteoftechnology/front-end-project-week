import styled from 'styled-components'
import { 
  alignItems, 
  justifyContent,
  height,
  width
} from 'styled-system'

export default styled.div`
  display: flex;
  ${alignItems}
  ${justifyContent}
  ${height}
  ${width}
`

