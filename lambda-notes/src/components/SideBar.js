import React, { Component } from 'react';
import { connect } from 'react-redux';

const SideBar = props => {
    return (
        <div>
            Hello
        </div>
    )
}

export default SideBar;