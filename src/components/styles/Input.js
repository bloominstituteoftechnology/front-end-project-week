import styled from 'styled-components';

const Input = styled.input`
  padding: 0.5em;
  margin: 0.5em;
  margin-left:0px;
  color: rgba(0,0,0,.9);
  font-weight:bold;
  background: #D3D3D3#;
  border: none;
  border-radius: 3px;
`;

export default Input;