import React from 'react';
import { StyledHeader, Container } from '../Styles';


export const Header = props => {
    return (
        <StyledHeader>
            <Container>
            HEADER
            </Container>
        </StyledHeader>
    )
}