import React from 'react';

const CreateNote = props => {
    return (
        <form>
            <input/>
            <textarea>
                Note Content
            </textarea>
        </form>
    )
}

export default CreateNote;