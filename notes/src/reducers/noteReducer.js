import { LOADING, SUCCESS, ERROR} from '../actions/noteActions';


initialState = {
    notes: [],
    loading:false,
    error:''
}

export default (state = initialState, action) => {
    switch(action.type){
        
    }
}