const initialState = [];

const noteReducer = (state = initialState, action) => {
    switch (action.type) {
        case 'a':
            return state;
        default:
            return state;
    } 
}
export default noteReducer;