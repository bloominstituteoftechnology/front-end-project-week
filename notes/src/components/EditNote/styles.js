import styled from 'styled-components';
import {
  StyledH3,
  ViewContainer,
  Button,
  StyledForm,
  StyledInput,
  StyledTextarea,
} from '../../mixins';

export const StyledEditView = styled(ViewContainer)``;

export const EditTitle = styled(StyledH3)``;

export const StyledEditForm = styled(StyledForm)``;

export const NoteTitleInput = styled(StyledInput)``;

export const NoteContentInput = styled(StyledTextarea)``;

export const SaveEditedButton = styled(Button)``;
