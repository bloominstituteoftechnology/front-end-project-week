import styled from 'styled-components';
import {
  StyledH3,
  ViewContainer,
  Button,
  StyledForm,
  StyledInput,
  StyledTextarea,
} from '../../mixins';

export const StyledCreateView = styled(ViewContainer)``;

export const CreateTitle = styled(StyledH3)``;

export const StyledCreateForm = styled(StyledForm)``;

export const NoteTitleInput = styled(StyledInput)``;

export const NoteContentInput = styled(StyledTextarea)``;

export const SaveNewButton = styled(Button)``;
