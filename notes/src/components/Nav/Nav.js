import React from 'react';

const Nav = props => {
    return (
        <div className="nav">
            <h1>Lambda Notes</h1>
            <button>View Your Notes</button>
            <button>+ Create New Note</button>
        </div>
    )
};

export default Nav;