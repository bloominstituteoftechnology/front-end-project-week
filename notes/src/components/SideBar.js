import React from "react";

const SideBar = props => (
    <div>
        <h2>Lambda Notes</h2>
        <button>View Notes</button>
        <button>+ Create New Note</button>
    </div>
);

export default SideBar;