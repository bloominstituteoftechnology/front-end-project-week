//import actions

const initialState = {
    noteList: [],
    // note: {
    //     tags: [],
    //     title: '',
    //     textBody: ''
    // }
    // editNote:
    // createNote:
    // listLoading:
    // noteLoading:
    // deleteNote:
    // deleteModal:
}

const noteReducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default noteReducer;