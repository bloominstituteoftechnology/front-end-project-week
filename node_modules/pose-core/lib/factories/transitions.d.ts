import { PoseMap, TransitionFactory, TransitionMap } from '../types';
declare const generateTransitions: <A>(poses: PoseMap<A>, defaultTransitions: Map<string, TransitionFactory<A> | TransitionMap<A>>) => PoseMap<A>;
export default generateTransitions;
