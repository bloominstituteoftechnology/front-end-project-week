import React from 'react';
import { NotesList } from './components';
import { Route } from 'react-router-dom';

import './App.css';

const App = () => {
  return (
    <div>
      <NotesList />
    </div>
  );
};

export default App;
